#include <stdio.h>
#include <stdlib.h>

typedef struct {
	int* valeurs;
	int taille;
}serie;

serie creation_serie(unsigned int n) {
	
	serie.valeurs = (int*) malloc(sizeof(int)*n);
	serie.taille = n;
	return 

int main() {

	serie t;

	int* ptr = creation_serie(n);

}


